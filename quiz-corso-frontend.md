# Quiz - Corso Frontend

20 domande a scelta multipla sugli argomenti del corso

---

## 1. Quale meta tag è obbligatorio per rendere una pagina responsive su dispositivi mobili?

a) `<meta charset="UTF-8">`  
b) `<meta name="viewport" content="width=device-width, initial-scale=1.0">` ✓  
c) `<meta name="description">`  
d) `<meta http-equiv="refresh">`

---

## 2. Quale tag HTML5 è utilizzato per definire il contenuto principale di una pagina?

a) `<content>`  
b) `<main>` ✓  
c) `<div>`  
d) `<primary>`

---

## 3. Quale attributo HTML è obbligatorio per rendere le immagini accessibili?

a) `class`  
b) `src`  
c) `alt` ✓  
d) `description`

---

## 4. In CSS, quale proprietà controlla lo spazio interno di un elemento (tra il contenuto e il bordo)?

a) `margin`  
b) `border-spacing`  
c) `padding` ✓  
d) `spacing`

---

## 5. Quale selettore CSS ha la massima specificità?

a) Selettore di classe (`.classe`)  
b) Selettore di ID (`#id`) ✓  
c) Selettore di elemento (`div`)  
d) Selettore universale (`*`)

---

## 6. Quale proprietà CSS permette di creare layout flessibili in una dimensione (riga o colonna)?

a) `display: grid`  
b) `display: block`  
c) `display: none`  
d) `display: flex` ✓

---

## 7. Nel Box Model CSS, qual è l'ordine corretto degli elementi dall'esterno verso l'interno?

a) Content, Border, Padding, Margin  
b) Margin, Padding, Border, Content  
c) Margin, Border, Padding, Content ✓  
d) Border, Margin, Padding, Content

---

## 8. Quante colonne ha il Grid System di Bootstrap 5?

a) 10 colonne  
b) 12 colonne ✓  
c) 16 colonne  
d) 24 colonne

---

## 9. Quale classe Bootstrap si usa per creare un bottone con lo stile primario?

a) `btn-primary` ✓  
b) `button-primary`  
c) `btn-main`  
d) `my-btn`

---

## 10. Quale breakpoint Bootstrap corrisponde a schermi di dimensione "large" (≥992px)?

a) `xl`  
b) `lg` ✓  
c) `md`  
d) `l`

---

## 11. In Bootstrap, quale classe si usa per nascondere un elemento su dispositivi mobili (small)?

a) `d-none d-sm-block`  
b) `d-sm-none` ✓  
c) `hide-sm`  
d) `mobile-hide`

---

## 12. Quale estensione di file è raccomandata per scrivere codice SASS con sintassi simile al CSS?

a) `.sasso`  
b) `.css`  
c) `.scss` ✓  
d) `.style`

---

## 13. In SASS, quale simbolo si usa per fare riferimento al selettore padre nel nesting (ad esempio .navigation .nav-item)?

a) `$`  
b) `@`  
c) `&` ✓  
d) `#`

---

## 14. Come si dichiara una variabile in SASS?

a) `var(--colore: blue)`  
b) `$colore: blue;` ✓  
c) `@colore: blue;`  
d) `let colore = blue;`

---

## 15. Quale parola chiave JavaScript si usa per dichiarare una variabile che NON può essere riassegnata?

a) `var`  
b) `let`  
c) `const` ✓  
d) `final`

---

## 16. Come si crea un OGGETTO in JavaScript con proprietà `nome` e `eta`?

a) `let persona = { nome: "Mario", eta: 30 }` ✓  
b) `let persona = [ nome: "Mario", età: 30 ]`  
c) `let persona = (nome: "Mario", età: 30)`  
d) `let persona = new Object("nome", "Mario", "età", 30)`

---

## 17. A cosa serve principalmente TypeScript?

a) Per rendere JavaScript più veloce nell'esecuzione  
b) Per aggiungere controllo dei tipi e type safety al codice JavaScript ✓  
c) Per sostituire completamente JavaScript con un nuovo linguaggio  
d) Per compilare JavaScript in linguaggio macchina

---

## 18. In React, quale hook si usa per gestire lo stato di un componente?

a) `useEffect`  
b) `useContext`  
c) `useState` ✓  
d) `useReducer`

---

## 19. Cosa rappresenta JSX in React?

a) Un linguaggio di programmazione separato da JavaScript  
b) Una libreria per la gestione dello stato  
c) Una sintassi che permette di scrivere codice HTML con una sintassti XML in JavaScript ✓  
d) Un framework CSS per React

---

## 20. Quale metodo JavaScript si usa per iterare su tutti gli elementi di un array eseguendo una funzione per ciascuno?

a) `maps()`  
b) `filter()`  
c) `forEach()` ✓  
d) `reduce()`

---

## Riepilogo Risposte Corrette

1. b) `<meta name="viewport" content="width=device-width, initial-scale=1.0">`
2. b) `<main>`
3. c) `alt`
4. c) `padding`
5. b) Selettore di ID (`#id`)
6. d) `display: flex`
7. c) Margin, Border, Padding, Content
8. b) 12 colonne
9. a) `btn-primary`
10. b) `lg`
11. b) `d-sm-none`
12. c) `.scss`
13. c) `&`
14. b) `$colore: blue;`
15. c) `const`
16. a) `let persona = { nome: "Mario", età: 30 }`
17. b) Per aggiungere controllo dei tipi e type safety al codice JavaScript
18. c) `useState`
19. c) Una sintassi che permette di scrivere codice HTML con una sintassti XML in JavaScript
20. c) `forEach()`
